# ============================================
# FTMS BACKEND - ENVIRONMENT CONFIGURATION
# ============================================

# Server Configuration
PORT=3001
NODE_ENV=development

# Database
FINANCE_MAIN_DATABASE_URL="postgresql://postgres:nerieannganda247@localhost:5432/db_name?schema=public"

# Redis Cache
REDIS_URL=redis://localhost:6379
    
# JWT Configuration (SHARED WITH HR AUTH MICROSERVICE)
JWT_SECRET=8f7b3a2c9d4e6f8a0b1c2d3e4f5g6h7i
JWT_EXPIRES_IN=8h

# External APIs - HR (Human Resources)
HR_AUTH_API_URL=http://localhost:3002
HR_API_EMPLOYEES_URL=https://backends-blue.vercel.app/api/clean/hr_employees
HR_API_PAYROLL_URL=https://backends-blue.vercel.app/api/clean/hr_payroll

# External APIs - Operations
OP_API_BUSTRIP_URL=https://backends-blue.vercel.app/api/clean/op_bus-trip-details

# External APIs - Audit Logs
AUDIT_LOGS_API_URL=http://localhost:4004
AUDIT_LOGS_API_KEY=FINANCE_DEFAULT_KEY

# CORS Configuration (Frontend URLs)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:4003,http://localhost:4004

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# Cache Configuration
REFRESH_INTERVAL=300
ENABLE_CACHE_SCHEDULER=false

# Authentication Configuration
# Set to 'false' to disable JWT/Bearer token authentication (for testing/development only)
# Set to 'true' to enable authentication (for production)
# Note: Server restart required after changing this value
ENABLE_AUTH=false
